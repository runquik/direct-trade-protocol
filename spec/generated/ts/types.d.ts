// GENERATED by sdk/scripts/build-schemas.ts from spec/schemas — do not edit.

/**
 * The company spine: identity, locations, keys, and attested credentials. Subject is the company itself. The genesis record MUST be signed by a root key listed in its own keys[] (self-certifying bootstrap). Any superseding record that changes keys[] MUST be signed by a root key.
 */

export interface CoreCompany {
  display_name: string;
  legal_name?: string | null;
  business_type:
    | "producer"
    | "brand"
    | "distributor"
    | "retailer"
    | "cooperative"
    | "financer"
    | "service_provider"
    | "agent"
    | "other";
  jurisdiction: string;
  locations: {
    location_id: string;
    label?: string | null;
    address: Address;
    gln?: string | null;
    roles?: ("ship_from" | "ship_to" | "bill_to" | "plant" | "warehouse" | "office" | "farm")[];
  }[];
  identifiers?: {
    duns?: string | null;
    tax_id?: string | null;
    /**
     * On-chain identity, if linked. A link, not the id.
     */
    near_account?: string | null;
  };
  /**
   * @minItems 1
   */
  keys: [Key, ...Key[]];
  kyb?: KybRef | null;
  certifications?: CertificationRef[];
  fsma_pcqi_on_file?: boolean;
  facility_allergens?: (
    "milk" | "eggs" | "fish" | "shellfish" | "tree_nuts" | "peanuts" | "wheat" | "soybeans" | "sesame"
  )[];
  data_vault_uri?: string | null;
  /**
   * This interface was referenced by `CoreCompany`'s JSON-Schema definition
   * via the `patternProperty` "^x_".
   */
  [k: string]: unknown;
}

export interface Address {
  line1?: string | null;
  line2?: string | null;
  city: string;
  /**
   * State/province code or name.
   */
  region: string;
  postal_code?: string | null;
  country: string;
}
/**
 * A signing key attached to a company or module. Rotation = add a new key, revoke the old one. A revoked key's signatures on records created before revoked_at remain valid.
 */

export interface Key {
  /**
   * Ed25519 public key, NEAR encoding: ed25519:<base58(32 bytes)>.
   */
  key_id: string;
  /**
   * Only root keys may change keys[] or write core.grant.
   */
  role: "root" | "delegate";
  label?: string | null;
  status: "active" | "revoked";
  /**
   * RFC 3339 UTC. Writers SHOULD include milliseconds and MUST use Z.
   */
  added_at: string;
  revoked_at?: string | null;
  /**
   * NEAR account (or sub-account) this key belongs to, if any.
   */
  near_account?: string | null;
}
/**
 * Know-Your-Business attestation anchoring a company to a legal entity.
 */

export interface KybRef {
  legal_name: string;
  tax_id?: string | null;
  jurisdiction: string;
  /**
   * e.g. stripe_identity, persona, manual, mock
   */
  provider: string;
  attestation_ref?: string | null;
  /**
   * RFC 3339 UTC. Writers SHOULD include milliseconds and MUST use Z.
   */
  issued_at: string;
  expires_at?: string | null;
  status: "pending" | "verified" | "expired" | "revoked";
}
/**
 * A certification claim. Never self-asserted: carries the issuer and a verification URL.
 */

export interface CertificationRef {
  cert_id: string;
  /**
   * e.g. USDA_ORGANIC, GAP, SQF, BRC, HACCP, NON_GMO, FAIR_TRADE, FSMA_COMPLIANT
   */
  cert_type: string;
  issuer: string;
  issuer_url?: string | null;
  /**
   * RFC 3339 UTC. Writers SHOULD include milliseconds and MUST use Z.
   */
  issued_at: string;
  expires_at?: string | null;
  verification_url?: string | null;
  status: "active" | "expired" | "revoked";
}

export type Scope = {
  /**
   * A namespace, or * for all.
   */
  namespace?: string;
  /**
   * <namespace>.<type>
   */
  type?: string;
  access: "read" | "write";
} & {
  [k: string]: unknown;
};

/**
 * A software identity with its own keys. Subject is the publisher company. The genesis record may be signed by a module root key listed in keys[] (self-certifying) or by a publisher root key. requested_scopes is advisory (consent screen); authority comes only from core.grant records.
 */

export interface CoreModule {
  /**
   * Module slug, unique per store.
   */
  module_id: string;
  name: string;
  /**
   * Company handle. NEAR account-id grammar so a later on-chain mapping is byte-compatible.
   */
  publisher_company_id: string;
  description?: string | null;
  homepage?: string | null;
  /**
   * @minItems 1
   */
  keys: [Key, ...Key[]];
  requested_scopes?: Scope[];
  /**
   * This interface was referenced by `CoreModule`'s JSON-Schema definition
   * via the `patternProperty` "^x_".
   */
  [k: string]: unknown;
}
/**
 * A signing key attached to a company or module. Rotation = add a new key, revoke the old one. A revoked key's signatures on records created before revoked_at remain valid.
 */

export type Scope1 =
  | {
      [k: string]: unknown;
    }
  | {
      [k: string]: unknown;
    };

/**
 * A company's permission to a module. Subject is the granting company. MUST be signed by a company root key with issuer.module_id null (modules never write grants). Revocation = a superseding record with status revoked; only the head of the chain counts. `write` implies `read`.
 */

export interface CoreGrant {
  /**
   * Module slug, unique per store.
   */
  module_id: string;
  /**
   * @minItems 1
   */
  scopes: [Scope, ...Scope[]];
  status: "active" | "revoked";
  expires_at?: string | null;
  note?: string | null;
  /**
   * This interface was referenced by `CoreGrant`'s JSON-Schema definition
   * via the `patternProperty` "^x_".
   */
  [k: string]: unknown;
}

/**
 * A buyer's declaration of what it wants to purchase. Subject is the buyer.
 */

export interface TradeIntent {
  /**
   * Company handle. NEAR account-id grammar so a later on-chain mapping is byte-compatible.
   */
  buyer_company_id: string;
  catalog_id?: string | null;
  goods: GoodsSpec;
  delivery: DeliverySpec;
  pricing: BuyerPricing;
  finance?: FinanceTerms | null;
  freight?: FreightTerms | null;
  /**
   * RFC 3339 UTC. Writers SHOULD include milliseconds and MUST use Z.
   */
  expires_at: string;
  status: "draft" | "posted" | "matched" | "contracted" | "fulfilled" | "settled" | "expired" | "cancelled";
  /**
   * This interface was referenced by `TradeIntent`'s JSON-Schema definition
   * via the `patternProperty` "^x_".
   */
  [k: string]: unknown;
}
/**
 * Describes goods requested or offered. Only the *_details block matching product_type is required.
 */

export interface GoodsSpec {
  /**
   * Dotted taxonomy, e.g. condiments.hot_sauce
   */
  category: string;
  product_name: string;
  description?: string | null;
  product_type: "commodity" | "branded" | "value_added";
  commodity_details?: {
    country_of_origin?: string;
    farming_practices?: string[];
    grade?: string | null;
    harvest_date?: string | null;
  } | null;
  branded_details?: {
    brand_name: string;
    sku: string;
    gtin?: string | null;
    upc?: string | null;
    manufacturer?: string | null;
  } | null;
  value_added_details?: {
    process_type: string;
    base_ingredients?: string[];
    processing_facility?: string | null;
  } | null;
  quantity: Quantity;
  quality?: {
    grade?: string | null;
    specifications?: string[];
  } | null;
  required_certifications?: string[];
  packaging?: string | null;
  shelf_life_days?: number | null;
  /**
   * This interface was referenced by `GoodsSpec`'s JSON-Schema definition
   * via the `patternProperty` "^x_".
   */
  [k: string]: unknown;
}
/**
 * Decimal string with up to 3 fractional digits (milliamount precision) plus a unit.
 */

export interface Quantity {
  amount: string;
  unit: "lb" | "kg" | "oz" | "ton" | "case" | "pallet" | "unit";
}

export interface DeliverySpec {
  destination: Address;
  destination_gln?: string | null;
  window: {
    /**
     * RFC 3339 UTC. Writers SHOULD include milliseconds and MUST use Z.
     */
    earliest: string;
    /**
     * RFC 3339 UTC. Writers SHOULD include milliseconds and MUST use Z.
     */
    latest: string;
  };
  method: "delivered" | "fob_origin" | "third_party_logistics";
  temperature_requirements?: ("ambient" | "refrigerated" | "frozen") | null;
  notes?: string | null;
  /**
   * This interface was referenced by `DeliverySpec`'s JSON-Schema definition
   * via the `patternProperty` "^x_".
   */
  [k: string]: unknown;
}

export interface BuyerPricing {
  ceiling_price_per_unit: Money;
  desired_quantity?: Quantity | null;
  /**
   * This interface was referenced by `BuyerPricing`'s JSON-Schema definition
   * via the `patternProperty` "^x_".
   */
  [k: string]: unknown;
}
/**
 * Decimal string with up to 6 fractional digits (microdollar precision). Never a JSON number.
 */

export interface Money {
  amount: string;
  currency: "USD" | "USDC";
}
/**
 * Payment timing and financing mode agreed for a trade. Policy defaults (LP APR, maturity caps, PACA 30-day cliff) are documented in SPEC.md §7 and are not schema-enforced.
 */

export interface FinanceTerms {
  payment_timing: "delivery_attestation" | "inspection_period_end";
  net_days: number;
  paca_covered: boolean;
  financing_mode: "escrow_only" | "lp_pool" | "open_account";
  liquidity_pool_id?: string | null;
  financer_company_id?: string | null;
  /**
   * Basis points.
   */
  finance_fee_bps?: number;
  /**
   * This interface was referenced by `FinanceTerms`'s JSON-Schema definition
   * via the `patternProperty` "^x_".
   */
  [k: string]: unknown;
}

export interface FreightTerms {
  payer: "buyer" | "seller";
  estimated_freight: Money;
  freight_allowance?: Money | null;
  quote_source: "project44" | "manual_estimate" | "carrier_quote";
  quote_ref?: string | null;
  /**
   * RFC 3339 UTC. Writers SHOULD include milliseconds and MUST use Z.
   */
  quoted_at: string;
  quote_expires_at?: string | null;
  booked_at_contract: boolean;
  /**
   * This interface was referenced by `FreightTerms`'s JSON-Schema definition
   * via the `patternProperty` "^x_".
   */
  [k: string]: unknown;
}

/**
 * A seller's proactive broadcast of available supply. Subject is the seller. MOQ lives in pack_structure.
 */

export interface TradeListing {
  /**
   * Company handle. NEAR account-id grammar so a later on-chain mapping is byte-compatible.
   */
  seller_company_id: string;
  lot_id?: string | null;
  goods: GoodsSpec;
  pack_structure: PackStructure;
  delivery: DeliverySpec;
  pricing: SellerPricing;
  finance?: FinanceTerms | null;
  freight?: FreightTerms | null;
  certifications?: CertificationRef[];
  /**
   * RFC 3339 UTC. Writers SHOULD include milliseconds and MUST use Z.
   */
  available_from: string;
  /**
   * RFC 3339 UTC. Writers SHOULD include milliseconds and MUST use Z.
   */
  expires_at: string;
  status: "draft" | "active" | "matched" | "contracted" | "expired" | "withdrawn";
  /**
   * This interface was referenced by `TradeListing`'s JSON-Schema definition
   * via the `patternProperty` "^x_".
   */
  [k: string]: unknown;
}
/**
 * Describes goods requested or offered. Only the *_details block matching product_type is required.
 */

export interface PackStructure {
  unit_size: Quantity;
  units_per_case: number;
  cases_per_pallet?: number | null;
  pallets_per_truckload?: number | null;
  moq: {
    quantity: Quantity;
    label?: string | null;
  };
  /**
   * This interface was referenced by `PackStructure`'s JSON-Schema definition
   * via the `patternProperty` "^x_".
   */
  [k: string]: unknown;
}

export interface SellerPricing {
  model: "flat" | "tiered" | "negotiable";
  asking_price_per_unit: Money;
  tiers?: {
    min_quantity: Quantity;
    max_quantity?: Quantity | null;
    price_per_unit: Money;
    label?: string | null;
  }[];
  /**
   * This interface was referenced by `SellerPricing`'s JSON-Schema definition
   * via the `patternProperty` "^x_".
   */
  [k: string]: unknown;
}
/**
 * Decimal string with up to 6 fractional digits (microdollar precision). Never a JSON number.
 */

/**
 * A targeted response to a posted intent or listing. Subject is the offerer; the target's owner is the counterparty.
 */

export interface TradeOffer {
  /**
   * Company handle. NEAR account-id grammar so a later on-chain mapping is byte-compatible.
   */
  offerer_company_id: string;
  target_type: "intent" | "listing";
  /**
   * root_id of the intent or listing
   */
  target_id: string;
  /**
   * Company handle. NEAR account-id grammar so a later on-chain mapping is byte-compatible.
   */
  target_owner_company_id: string;
  goods: GoodsSpec;
  delivery: DeliverySpec;
  finance?: FinanceTerms | null;
  freight?: FreightTerms | null;
  price_per_unit: Money;
  total_price: Money;
  certifications?: CertificationRef[];
  /**
   * RFC 3339 UTC. Writers SHOULD include milliseconds and MUST use Z.
   */
  expires_at: string;
  status: "submitted" | "shortlisted" | "accepted" | "rejected" | "expired" | "retracted";
  /**
   * This interface was referenced by `TradeOffer`'s JSON-Schema definition
   * via the `patternProperty` "^x_".
   */
  [k: string]: unknown;
}
/**
 * Describes goods requested or offered. Only the *_details block matching product_type is required.
 */

/**
 * Binding record of agreed trade terms. Subject is the buyer (it is the buyer's payable); the seller reads it as a counterparty. Created by whichever party accepted the offer; may also be created directly under a standing agreement or by mutual direct agreement.
 */

export interface TradeContract {
  /**
   * Company handle. NEAR account-id grammar so a later on-chain mapping is byte-compatible.
   */
  buyer_company_id: string;
  /**
   * Company handle. NEAR account-id grammar so a later on-chain mapping is byte-compatible.
   */
  seller_company_id: string;
  intent_id?: string | null;
  listing_id?: string | null;
  offer_id?: string | null;
  standing_agreement_id?: string | null;
  lot_id?: string | null;
  buyer_po_number?: string | null;
  goods: GoodsSpec;
  delivery: DeliverySpec;
  finance?: FinanceTerms | null;
  freight?: FreightTerms | null;
  price_per_unit: Money;
  total_value: Money;
  /**
   * On-chain escrow reference; null off-chain.
   */
  escrow_ref?: string | null;
  dispute_window_hours: number;
  arbitrator_company_id?: string | null;
  status:
    | "active"
    | "in_fulfillment"
    | "delivered"
    | "settled"
    | "disputed"
    | "resolved_buyer"
    | "resolved_seller"
    | "cancelled";
  /**
   * This interface was referenced by `TradeContract`'s JSON-Schema definition
   * via the `patternProperty` "^x_".
   */
  [k: string]: unknown;
}
/**
 * Describes goods requested or offered. Only the *_details block matching product_type is required.
 */

/**
 * Delivery record for a contract. Subject is the seller (who attests shipment/delivery); the buyer attests receipt by superseding this record with buyer_attestation filled in. Structured evidence (BOL, temperature logs, inspection) may be attached under x_evidence in v0.2; a first-class evidence type is a v0.3 candidate.
 */

export interface TradeFulfillment {
  /**
   * Writer-assigned UUID (v4 or v7), lowercase.
   */
  contract_id: string;
  /**
   * Company handle. NEAR account-id grammar so a later on-chain mapping is byte-compatible.
   */
  seller_company_id: string;
  /**
   * Company handle. NEAR account-id grammar so a later on-chain mapping is byte-compatible.
   */
  buyer_company_id: string;
  /**
   * RFC 3339 UTC. Writers SHOULD include milliseconds and MUST use Z.
   */
  delivered_at: string;
  quantity_delivered: Quantity;
  seller_attestation: Attestation;
  buyer_attestation: Attestation | null;
  deductions?: {
    reason: string;
    amount: Money;
  }[];
  status: "seller_attested" | "buyer_attested" | "complete" | "disputed";
  /**
   * This interface was referenced by `TradeFulfillment`'s JSON-Schema definition
   * via the `patternProperty` "^x_".
   */
  [k: string]: unknown;
}
/**
 * Decimal string with up to 3 fractional digits (milliamount precision) plus a unit.
 */

export interface Attestation {
  /**
   * Company handle. NEAR account-id grammar so a later on-chain mapping is byte-compatible.
   */
  company_id: string;
  /**
   * RFC 3339 UTC. Writers SHOULD include milliseconds and MUST use Z.
   */
  attested_at: string;
  /**
   * Writer-assigned UUID (v4 or v7), lowercase.
   */
  record_id: string;
  notes?: string | null;
}
/**
 * Decimal string with up to 6 fractional digits (microdollar precision). Never a JSON number.
 */

/**
 * Final accounting for a contract. Subject is the buyer (payer); the seller is the counterparty. Terminal: corrections are a new settlement whose `corrects` names this one.
 */

export interface TradeSettlement {
  /**
   * Writer-assigned UUID (v4 or v7), lowercase.
   */
  contract_id: string;
  /**
   * Writer-assigned UUID (v4 or v7), lowercase.
   */
  fulfillment_id: string;
  /**
   * Company handle. NEAR account-id grammar so a later on-chain mapping is byte-compatible.
   */
  buyer_company_id: string;
  /**
   * Company handle. NEAR account-id grammar so a later on-chain mapping is byte-compatible.
   */
  seller_company_id: string;
  gross_amount: Money;
  deductions: {
    reason: string;
    amount: Money;
  }[];
  net_amount: Money;
  escrow_release_tx?: string | null;
  /**
   * finance.settlement_event root_ids that moved the money.
   */
  settlement_event_ids?: string[];
  corrects?: string | null;
  /**
   * RFC 3339 UTC. Writers SHOULD include milliseconds and MUST use Z.
   */
  settled_at: string;
  /**
   * This interface was referenced by `TradeSettlement`'s JSON-Schema definition
   * via the `patternProperty` "^x_".
   */
  [k: string]: unknown;
}
/**
 * Decimal string with up to 6 fractional digits (microdollar precision). Never a JSON number.
 */

/**
 * A framework governing a series of trades. Subject is the proposer; the other party is the counterparty. Countersignature = the counterparty supersedes the record, appending to signatures[] and setting status active.
 */

export interface TradeStandingAgreement {
  /**
   * Company handle. NEAR account-id grammar so a later on-chain mapping is byte-compatible.
   */
  proposer_company_id: string;
  proposer_role: "buyer" | "seller";
  /**
   * Company handle. NEAR account-id grammar so a later on-chain mapping is byte-compatible.
   */
  buyer_company_id: string;
  /**
   * Company handle. NEAR account-id grammar so a later on-chain mapping is byte-compatible.
   */
  seller_company_id: string;
  goods: GoodsSpec;
  terms: {
    /**
     * RFC 3339 UTC. Writers SHOULD include milliseconds and MUST use Z.
     */
    period_start: string;
    /**
     * RFC 3339 UTC. Writers SHOULD include milliseconds and MUST use Z.
     */
    period_end: string;
    volume_commitment?: {
      min_quantity_per_period?: Quantity;
      period?: "monthly" | "quarterly" | "annual";
      committed_total?: Quantity;
    } | null;
    pricing: SellerPricing;
    delivery_cadence?: string | null;
    renewal: "auto" | "manual" | "none";
  };
  signatures: Attestation[];
  status: "proposed" | "countered" | "active" | "completed" | "terminated";
  /**
   * This interface was referenced by `TradeStandingAgreement`'s JSON-Schema definition
   * via the `patternProperty` "^x_".
   */
  [k: string]: unknown;
}
/**
 * Describes goods requested or offered. Only the *_details block matching product_type is required.
 */

/**
 * A receivable. Subject is the seller (AR); the buyer is the counterparty. Issued by the seller or a module holding finance write from the seller. assigned_to_company_id is set when an advance funds and the receivable is assigned to the financer — the field a factor's lockbox exists to replace.
 */

export interface FinanceInvoice {
  invoice_number: string;
  /**
   * Company handle. NEAR account-id grammar so a later on-chain mapping is byte-compatible.
   */
  seller_company_id: string;
  /**
   * Company handle. NEAR account-id grammar so a later on-chain mapping is byte-compatible.
   */
  buyer_company_id: string;
  /**
   * Writer-assigned UUID (v4 or v7), lowercase.
   */
  contract_id: string;
  fulfillment_id?: string | null;
  /**
   * @minItems 1
   */
  line_items: [
    {
      description: string;
      sku?: string | null;
      quantity: Quantity;
      unit_price: Money;
      amount: Money;
    },
    ...{
      description: string;
      sku?: string | null;
      quantity: Quantity;
      unit_price: Money;
      amount: Money;
    }[]
  ];
  subtotal: Money;
  deductions: {
    reason: string;
    amount: Money;
  }[];
  total: Money;
  /**
   * RFC 3339 UTC. Writers SHOULD include milliseconds and MUST use Z.
   */
  issued_at: string;
  /**
   * RFC 3339 UTC. Writers SHOULD include milliseconds and MUST use Z.
   */
  due_at: string;
  payment_terms: {
    net_days: number;
    paca_covered: boolean;
    early_pay_discount_bps?: number | null;
  };
  status: "draft" | "issued" | "acknowledged" | "disputed" | "partially_paid" | "paid" | "void";
  paid_amount: Money;
  settlement_event_ids: string[];
  assigned_to_company_id?: string | null;
  /**
   * This interface was referenced by `FinanceInvoice`'s JSON-Schema definition
   * via the `patternProperty` "^x_".
   */
  [k: string]: unknown;
}
/**
 * Decimal string with up to 3 fractional digits (milliamount precision) plus a unit.
 */

/**
 * A financer's offer to advance cash against an invoice. Subject is the seller; the financer is the counterparty and the issuer (via its module). pricing_basis lists the protocol records the decision was made from — proof that no out-of-band intake was needed.
 */

export interface FinanceAdvanceOffer {
  /**
   * Writer-assigned UUID (v4 or v7), lowercase.
   */
  invoice_id: string;
  /**
   * Company handle. NEAR account-id grammar so a later on-chain mapping is byte-compatible.
   */
  seller_company_id: string;
  /**
   * Company handle. NEAR account-id grammar so a later on-chain mapping is byte-compatible.
   */
  financer_company_id: string;
  advance_amount: Money;
  /**
   * advance_amount as bps of invoice total
   */
  advance_bps: number;
  fee: {
    /**
     * Basis points.
     */
    fee_bps: number;
    /**
     * Basis points.
     */
    apr_bps: number;
    fixed_fee?: Money | null;
  };
  repayment: {
    source: "buyer_payment" | "seller";
    /**
     * RFC 3339 UTC. Writers SHOULD include milliseconds and MUST use Z.
     */
    due_at: string;
  };
  recourse: "full" | "limited" | "none";
  /**
   * @minItems 1
   */
  pricing_basis: [
    {
      /**
       * Writer-assigned UUID (v4 or v7), lowercase.
       */
      record_id: string;
      /**
       * <namespace>.<type>
       */
      type: string;
      note?: string | null;
    },
    ...{
      /**
       * Writer-assigned UUID (v4 or v7), lowercase.
       */
      record_id: string;
      /**
       * <namespace>.<type>
       */
      type: string;
      note?: string | null;
    }[]
  ];
  /**
   * RFC 3339 UTC. Writers SHOULD include milliseconds and MUST use Z.
   */
  expires_at: string;
  status: "offered" | "accepted" | "declined" | "expired" | "withdrawn";
  /**
   * This interface was referenced by `FinanceAdvanceOffer`'s JSON-Schema definition
   * via the `patternProperty` "^x_".
   */
  [k: string]: unknown;
}
/**
 * Decimal string with up to 6 fractional digits (microdollar precision). Never a JSON number.
 */

/**
 * A funded advance, created only from an accepted advance_offer. Subject is the seller; the financer is the counterparty and owns every transition, each of which MUST cite settlement events.
 */

export interface FinanceAdvance {
  /**
   * Writer-assigned UUID (v4 or v7), lowercase.
   */
  advance_offer_id: string;
  /**
   * Writer-assigned UUID (v4 or v7), lowercase.
   */
  invoice_id: string;
  /**
   * Company handle. NEAR account-id grammar so a later on-chain mapping is byte-compatible.
   */
  seller_company_id: string;
  /**
   * Company handle. NEAR account-id grammar so a later on-chain mapping is byte-compatible.
   */
  financer_company_id: string;
  principal: Money;
  fee: {
    /**
     * Basis points.
     */
    fee_bps: number;
    /**
     * Basis points.
     */
    apr_bps: number;
    fixed_fee?: Money | null;
  };
  /**
   * RFC 3339 UTC. Writers SHOULD include milliseconds and MUST use Z.
   */
  funded_at: string;
  /**
   * Writer-assigned UUID (v4 or v7), lowercase.
   */
  funding_event_id: string;
  /**
   * RFC 3339 UTC. Writers SHOULD include milliseconds and MUST use Z.
   */
  maturity_at: string;
  repaid_amount: Money;
  outstanding: Money;
  repayment_event_ids?: string[];
  status: "funded" | "partially_repaid" | "repaid" | "defaulted" | "written_off";
  /**
   * This interface was referenced by `FinanceAdvance`'s JSON-Schema definition
   * via the `patternProperty` "^x_".
   */
  [k: string]: unknown;
}
/**
 * Decimal string with up to 6 fractional digits (microdollar precision). Never a JSON number.
 */

/**
 * An immutable money movement. Subject is the payer; the payee is the counterparty. No status: corrections are a compensating event whose `reverses` names this one.
 */

export interface FinanceSettlementEvent {
  kind:
    | "buyer_payment"
    | "advance_funding"
    | "advance_repayment"
    | "fee"
    | "refund"
    | "escrow_release"
    | "adjustment";
  /**
   * Company handle. NEAR account-id grammar so a later on-chain mapping is byte-compatible.
   */
  from_company_id: string;
  /**
   * Company handle. NEAR account-id grammar so a later on-chain mapping is byte-compatible.
   */
  to_company_id: string;
  amount: Money;
  /**
   * RFC 3339 UTC. Writers SHOULD include milliseconds and MUST use Z.
   */
  occurred_at: string;
  rail: "mock" | "ach" | "wire" | "card" | "check" | "usdc_near";
  rail_ref?: string | null;
  references: {
    invoice_id?: string | null;
    advance_id?: string | null;
    contract_id?: string | null;
    trade_settlement_id?: string | null;
  };
  reverses: string | null;
  memo?: string | null;
  /**
   * This interface was referenced by `FinanceSettlementEvent`'s JSON-Schema definition
   * via the `patternProperty` "^x_".
   */
  [k: string]: unknown;
}
/**
 * Decimal string with up to 6 fractional digits (microdollar precision). Never a JSON number.
 */

/**
 * An FSMA 204 Critical Tracking Event keyed by a Traceability Lot Code. Subject is the actor. Loose in v0.2 (additional properties allowed) — this namespace is exercised in a later sprint; an EPCIS 2.0 export mapping is planned.
 */

export interface TraceabilityCte {
  cte_type: "growing" | "creating" | "receiving" | "transforming" | "shipping";
  /**
   * Traceability Lot Code (opaque in v0.2).
   */
  lot_id: string;
  output_lot_id?: string | null;
  input_lot_ids?: string[];
  /**
   * Company handle. NEAR account-id grammar so a later on-chain mapping is byte-compatible.
   */
  actor_company_id: string;
  actor_gln?: string | null;
  source_gln?: string | null;
  dest_gln?: string | null;
  quantity: Quantity;
  commodity?: string | null;
  variety?: string | null;
  /**
   * When the physical event happened (the envelope created_at is when it was recorded).
   */
  event_date: string;
  contract_id?: string | null;
  notes?: string | null;
  [k: string]: unknown;
}
/**
 * Decimal string with up to 3 fractional digits (milliamount precision) plus a unit.
 */

/**
 * Tamper-evident anchor for an off-chain lot document (Certificate of Analysis etc.) by content hash. Subject is the anchoring company.
 */

export interface TraceabilityCoaAnchor {
  /**
   * Company handle. NEAR account-id grammar so a later on-chain mapping is byte-compatible.
   */
  company_id: string;
  lot_id: string;
  cert_type: string;
  issuer: string;
  /**
   * SHA-256 hex or IPFS CIDv1
   */
  doc_hash: string;
  doc_uri_hint?: string | null;
  issued_at?: string | null;
  expires_at?: string | null;
  [k: string]: unknown;
}

