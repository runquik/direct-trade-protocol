{
  "mcpServers": {
    "dtp": {
      "command": "npx",
      "args": ["tsx", "mcp-server/src/index.ts"],
      "cwd": "mcp-server",
      "env": {
        "NEAR_NETWORK_ID": "testnet",
        "NEAR_NODE_URL": "https://test.rpc.fastnear.com"
      }
    }
  }
}
