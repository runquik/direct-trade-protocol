# DTP_ARCHIVE_REFERENCE_ONLY: deliberately no build stages.
# This frozen NEAR MCP client is not a foundation runtime or deployable artifact.
# Historical recipe preserved verbatim in Dockerfile.reference; it is unsafe to revive.
# See ../docs/foundation/legacy-adapter-boundary.md. No live deployment was changed.
