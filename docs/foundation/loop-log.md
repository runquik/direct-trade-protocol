# DTP foundation loop log

## Loop 1: baseline and contract freeze

- Plan approved; branch created from fetched main with matching reviewed source tree.
- Architecture reviewer, datatype implementer and validation auditor assigned distinct work; no self-approval.
- User approved synthetic user-controlled recovery; production custody remains unselected.
- Architecture review identified stale resolver proofs, returning old authority after transfer, and generic-write operation bypass as explicit boundaries.
- Foundation evidence graph starts pending. Current implementation work is bounded datatypes; no runtime or wire integration is claimed yet.
- Existing uncommitted files and historical release evidence preserved.

## Loop 2: independent helper review

- Datatype contradictory causation references corrected by their implementer and independently retested. Helper accepted; record integration remains open.
- Release graph review found unrelated gate closure, review-only closure, missing executable test inputs, renamed smoke checks and omitted transitive inputs. Root corrected them; independent graph probes pass. No broad release approval follows from this.
- Identity review caught timestamp headroom inconsistency, malformed predecessor acceptance and nested accessor execution. Root fixed them; independent reviewer accepted16helper tests before subsequent enrollment additions, which require another review.
- Semantic admission helper passed20tests including independently authored adversarial probes. Root/kernel must bind approvals to exact intent plus evaluated plan, not intent alone.
- Agency authority helper now covers dual-company delegation, ancestor cumulative budgets and exact-plan approvals. Independent review caught an insufficiently scoped receipt retry; fix is pending independent recheck.
- Root added an indexed identity registry with transactional enrollment, control history and lease persistence;4PGlite adapter tests pass. Real PostgreSQL and host integration are not yet qualified.
- Legacy adapter maintainer implemented default-entry refusal to enforce previously documented reference-only status. Historical recipes preserved. Independent review pending; no running service changed.
- Discovery helper is in independent review; minimum/maximum terms and explicit authority locator are being completed before acceptance.
- F0-F10 acceptance evidence remains pending. These subchecks do not close integrated packages or the external-builder gate.

## Loop 3: adversarial fixes and real database qualification

- Independent review reproduced signed-input mutation across async boundaries in identity verification and registry history. The source owner added bounded, descriptor-safe copying before asynchronous work. Audience pinning and historical transition receipts were separately checked; no current authority is returned by replaying an old enrollment or transition.
- Authority receipt scope, discovery interval compatibility and expired provider-window confirmation findings were fixed by their source owners and independently retested. The corresponding library slices passed, not their full integrated release packages.
- Change-consumer freshness and snapshot retry findings were fixed by the change implementation owner and independently retested. No live authorized feed is claimed yet.
- Legacy adapter refusal and CI test wiring were independently reviewed. Dependency vulnerabilities in the preserved legacy trees were not described as repaired.
- Docker startup failed on inaccessible Windows runtime socket artifacts. With explicit user approval, only the affected runtime directories were moved to timestamped backups. Docker29.1.3 and existing services recovered without resetting settings or deleting volumes/images.
- A separate synthetic PostgreSQL17.11 container was created on loopback15439. Seven identity tests, including physical JSONB object assertions, per-person lock isolation and real SQL rollback with a fresh registry instance, passed independent review. This is not backup/restore or full host qualification.
- The indexed business store passed14owner and7independent PGlite tests covering transactional rollback, CAS, scoped foreign references, cumulative budgets, retries and current read denial. Concrete authentication hooks and real PostgreSQL business-store concurrency remain open.
- Existing baseline suites were rerun with pinned Node22.23.2:103default and116candidate tests passed, with zero skips. Newly added source continues to invalidate foundation evidence until its bounded checks are rerun against the final input hashes.
- The next loop connects actual person signatures to transaction-bound command acceptance and qualifies the business store on real PostgreSQL. Service principals, other authorization hooks, full operational qualification and the actual external builder remain required; no full F0-F10 green claim is made.

## Loop 4: authenticated execution deadline and explicit defect closure

- The business store now requires a final trusted callback, rechecks grants and approval expiry at a fresh non-regressing time against its original locked authority row, preserves the original grant on receipt retry, and never persists a second charge. Five owner and four independently authored tests qualify this delta.
- The person-authentication owner implemented real operational Ed25519 quorum, pinned resolver leases, host-generated request-bound challenges, shared durable person checkpoints, transaction-bound nonce consumption and a deferred SQL commit deadline. Other governance/read/module/agency/accounting hooks remain explicitly synthetic in its fixtures.
- An independent real PostgreSQL test proved that a JavaScript callback stalled beyond the credential deadline cannot commit: business effects, receipt, outbox, checkpoint and challenge consumption all roll back. Two simultaneous uses of one signed challenge commit once.
- The coordinator identified a possible queue-time/clock-skew conflation; the reviewer reproduced it with an actual1250ms shared-person lock wait. The original request was incorrectly rejected despite a live proof. Finding `F9-auth-lock-clock` was recorded before closure. The source owner corrected clock sampling without extending any validity window; the independent unchanged contention test and hostile clock controls then passed.
- A complete source-frozen runner pass recorded15bounded test checks:280local and22real PostgreSQL tests,302in total, all with zero skips. These are actual per-command execution counts, not302independent safety guarantees. The full F0-F10 acceptance commands and reviews remain separate and unqualified.
- All ten foundation source modules passed cached Deno2.9.6 checking and full pinned-Node TypeScript checking after the correction. Five historical schema/vector/migration generators produced no tracked output drift. The release-policy author and independent native-crypto review jointly checked nine historical compatibility cases without relabeling signed formats.
- The specific timing finding is closed only through a current source-bound PostgreSQL retest plus the independent reviewer's own scoped observation. That observation is not whole-F9 approval. Full authenticated host composition, service identity, remaining record/profile/feed/discovery/coordination integration, operational qualification and actual external-builder participation still prevent release readiness.
