# Release artifact hashes describe exact downloaded/generated bytes. Prevent
# checkout newline conversion from invalidating the portable evidence bundle.
docs/release/** -text
# Foundation evidence also hashes the original artifact bytes.
docs/foundation/runs/** -text
docs/foundation/person-authentication-review.md -text
# Historical deployment recipes are preserved byte-for-byte, including CRLF.
remote-mcp-server/Dockerfile.reference -text whitespace=cr-at-eol
remote-mcp-server/railway.toml.reference -text whitespace=cr-at-eol
LICENSE -text
