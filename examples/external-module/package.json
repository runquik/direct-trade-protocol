{
  "name": "dtp-external-module-example",
  "private": true,
  "type": "module",
  "description": "A read-only inventory exception module built outside the protocol repository against the packed @dtp/sdk and a configured disposable host",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22.23.2 <23"
  },
  "scripts": {
    "prepare-host": "node prepare.mjs",
    "seed": "node seed.mjs",
    "read": "node read.mjs"
  },
  "dependencies": {
    "@dtp/sdk": "file:../../sdk/dtp-sdk-0.2.0.tgz"
  }
}
